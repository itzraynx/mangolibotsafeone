# ═══════════════════════════════════════════════════════════════════════════════
# NOVAGEN BOT - Requirements
# Python Hosting Compatible
# ═══════════════════════════════════════════════════════════════════════════════

# Discord Bot Framework
discord.py>=2.0.0

# HTTP Requests
requests>=2.28.0
aiohttp>=3.8.0

# Flask for Dashboard API
flask
flask-cors

# ── Voice Support (for /voicejoin commands) ────────────────────────────────────
# PyNaCl enables encrypted voice connections.
PyNaCl>=1.5.0
# davey is required by discord.py 2.7+ for voice audio.
davey==0.1.6
# ffmpeg must ALSO be installed on the host system (not a pip package):
#   Linux:   sudo apt install ffmpeg
#   Windows: download from https://ffmpeg.org and add to PATH

# ── Music (for /play, /queue, etc.) ────────────────────────────────────────────
# wavelink is the Python client for Lavalink (the audio server).
wavelink>=3.5.0
# NOTE: music ALSO requires a running Lavalink server (a separate Java program).
#   Download: https://github.com/lavalink-devs/Lavalink
#   Configure the address/password at the top of music.py
